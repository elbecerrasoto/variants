# RefSeq ID of reference genome
reference_id:
  "GCA_013166975.1" # BL12 e. coli

# TSV of input files
# The columns are
# 1. Path to forward reads
# 2. Path to reverse reads
# 3. Sample name
input:
  "tests/input.tsv" # names to give to the outputs

out_results:
  "tests/results"

variants_pval: 0.01
variants_freq: 0.1
