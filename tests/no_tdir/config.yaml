# RefSeq ID of reference genome
reference_id:
  "GCA_013166975.1" # BL12 e. coli

target_directory:
  "tests/no_tdir/results" # Unused, Input files take precedence

forward_mark:
  "_R1"

reverse_mark:
  "_R2"

# Input files, if not provided, generated using target directory and marks
names:
  "tests/no_tdir/results/input/names.txt"
forwards:
  "tests/no_tdir/results/input/forwards.txt"
reverses:
  "tests/no_tdir/results/input/reverses.txt"

out_results:
  "tests/no_tdir/results"
out_alignments:
  "tests/no_tdir/results/alignments"
out_reference:
  "tests/no_tdir/results/reference"

bowtie2_threads: 12

variants_pval: 0.01
variants_freq: 0.1
